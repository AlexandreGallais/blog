import type { Routes } from '@angular/router';
import { PageSymbolComponent } from './page-symbol.component';

export const pageSymbolRoutes: Routes = [
  {
    path: '',
    component: PageSymbolComponent,
  },
];
